import React from "react"
import styled from "@emotion/styled"

const Header = () => {
    return(
        <Head>
            <h2>Providing Medicine At Less than 80% Cost, Making World a Better Place</h2>
            <h4>Medical Central makes medicines accessible for everyone</h4>
        </Head>
        )}

const Head = styled.div`
     text-align: center; 
    margin: 6rem 0rem;
    font-family: Georgia, 'Times New Roman', Times, serif;
    
`

export default Header